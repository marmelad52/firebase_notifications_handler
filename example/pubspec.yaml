name: notifications_handler_demo
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  easy_container: ^1.0.5+1
  firebase_core: ^4.1.0
  firebase_notifications_handler:
    path: ../
  flutter:
    sdk: flutter
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
